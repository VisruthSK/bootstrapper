# Configure GitHub Actions Defaults

Sets up standard GitHub Actions used by this package template and
updates workflow references. DOES NOT setup formatting, that is owned by
[`setup_formatter()`](https://bootstrapper.visruth.com/reference/setup_formatter.md).

## Usage

``` r
setup_gha()
```

## Value

Invisibly returns `NULL`.
